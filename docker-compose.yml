version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: networkup-rag-backend
    ports:
      - "3004:3000"
    env_file:
      - .env
    environment:
      - PORT=3004
    restart: unless-stopped